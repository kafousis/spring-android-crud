pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()

        // toasty lib needs this
        maven { url "https://jitpack.io" }
    }
}
rootProject.name = "SpringAndroidCrud"
include ':app'
